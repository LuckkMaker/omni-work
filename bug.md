列计划，解决以下问题
1. Flash 页
- Fill Memory 点击填充失败
- 数据tab，加载hex文件后，数据右侧会有对应的ASCII码显示，但是ASCII码显示不对，你对比JLink的显示，目前的ASCII码显示是错的

2. Monitor 页
- 右侧变量，变量列表移除value
- watch窗口，增大 Value列宽度，避免显示不全，以及数据变化时，导致watch窗口的Value列宽度不断变化，造成界面抖动。可以压缩Name列宽度，增大Value列宽度